<h1>{{title}}</h1>
<h2>{{book.name}}</h2>
<h3>by {{book.author.name}}</h3>
<p>{{book.summary}}</p>
{{#get_length book_instances}}
<p><strong>Delete the following book instances before attempting to delete this book:</strong></p>
<div style="margin-left:20px;margin-top:20px">
<h4>Copies</h4>
<dl>
  {{#each book_instances}}
  <dt><a href={{this.url}}>{{this.title}}</a></dt>
  <dd>{{this.imprint}}</dd>
  <dd>{{this.status}}</dd>
  {{/each}}
</dl>
</div>
{{/get_length}}
<p>Do you really want to delete this book?</p>
<form action="" method="POST">
  <div class="form-group">
    <input type="hidden" id="bookid" class="form-control" name="bookid" required value={{book._id}}>
  </div>
  <button class="btn btn-primary" type="submit">Delete</button>
</form>
<hr>
<p><a href={{delete_page book.url}}>Delete book</a></p>